# -----------------------------------------------------------------
# vue-ssr-jod : Example environment file
# Copy this file to ".env" and adjust values depending on environment (dev/prod).
# -----------------------------------------------------------------

# Server
PORT=8000                 # SSR server port (e.g. 8000 in local, 3000 on Render)

# Canonicalization
# Canonical host that the site should enforce.
# Production example: www.braziliandev.com
# In local dev use "localhost" to avoid redirects to the real domain.
CANONICAL_HOST=localhost

# FORCE_HTTPS controls protocol redirection:
#   true  = redirect http:// -> https://
#   false = keep protocol as requested (useful in local dev)
FORCE_HTTPS=false

# FORCE_WWW controls the use of "www":
#   true  = always redirect to www.example.com
#   false = always redirect to example.com (no www)
FORCE_WWW=false

# SEO base URLs
# BASE_URL is used in sitemap.xml, robots.txt and canonical links.
# Production example: https://www.braziliandev.com
# Local dev: http://localhost:8000
BASE_URL=http://localhost:8000

# Robots.txt control
# ROBOTS_ALLOW sets robots.txt behavior:
#   all  = allow search engine indexing
#   none = disallow indexing (useful in staging/private envs)
ROBOTS_ALLOW=all

# Cache policy
# CACHE_HTML_SECONDS defines the Cache-Control header for HTML pages:
#   0    = no cache (always revalidated)
#   120  = cache publicly for 120 seconds
CACHE_HTML_SECONDS=120
